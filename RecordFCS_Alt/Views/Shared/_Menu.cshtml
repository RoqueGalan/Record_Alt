@using RecordFCS_Alt.Helpers

<nav id="menuUsuario" class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse-1">
                <span class="sr-only">Pestaña de Navegación</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <div class="logoFundacion">
                <img src="~/Content/img/home/carrusel/logo1.jpg" alt="Fundación Carlos Slim" class="imgSaludo" />
            </div>
           @*@Html.NoEncodeActionLink("<font size='6'>R</font><font size='3'> E C O R D</font>", "Inicio de RECORD", "Index", "Home", null, htmlAttributes: new { data_toggle = "tooltip", @class = "navbar-brand" })*@

        </div>
      

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navbar-collapse-1">

            @if (IsAuthenticated)
            {
                //barras izquierda|
                <ul class="nav navbar-nav">

                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class='fa fa-search'></i> Consulta <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            @Html.MenuItemIcon("<i class='fa fa-search'></i> Buscador Avanzado", "Buscador Avanzado", "Index", "Buscador")
                            @Html.MenuItemIcon("<i class='fa fa-list'></i> Listados", "Listados", "Index", "Listado")
                            @Html.MenuItemIcon("<i class='fa fa-list-alt'></i> Reportes", "Reportes", "Index", "Reporte")
                        </ul>
                    </li>



                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><i class='fa fa-connectdevelop'></i> Control de Obra <span class="caret"></span></a>
                        <ul class="dropdown-menu" role="menu">
                            @if (User.IsInRole("ObraRegistrar"))
                            {
                                @Html.MenuItemIcon("<i class='fa fa-edit'></i> Registro de Obra", "Registro de Obra", "Registro", "Obra")
                            }

                        </ul>
                    </li>






                    <li class="menu-item dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-gear"></i> Configuración<span class="caret"></span></a>

                        <ul class="dropdown-menu">

                            @if (User.IsInRole("SeguridadVer"))
                            {
                                <li class="menu-item dropdown dropdown-submenu">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-shield"></i> Seguridad Sistema</a>

                                    <ul class="dropdown-menu">
                                        @if (User.IsInRole("TipoPermisoVer"))
                                        {
                                            @Html.MenuItemIcon("<i class='fa fa-key'></i> Permisos", "Catalogo Permisos", "Index", "TipoPermiso")
                                        }
                                        @if (User.IsInRole("UsuarioVer"))
                                        {
                                            @Html.MenuItemIcon("<i class='fa fa-group'></i> Usuarios", "Usuarios", "Index", "Usuario")
                                        }
                                    </ul>

                                </li>
                            }

                            @if (User.IsInRole("ConfigVer"))
                            {
                                <li class="menu-item dropdown dropdown-submenu">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-gear"></i> Configuración Obras</a>

                                    <ul class="dropdown-menu">
                                        @if (User.IsInRole("TipoObraVer"))
                                        {
                                            @Html.MenuItemIcon("<i class='fa fa-picture-o'></i> Tipos de Obra", "Tipos de Obra", "Index", "TipoObra")
                                        }

                                        @if (User.IsInRole("CatVer"))
                                        {
                                            @Html.MenuItemIcon("<i class='fa fa-th-large'></i> Catalogos", "Catalogos", "Index", "TipoAtributo")
                                        }

                                    </ul>

                                </li>
                            }


                        </ul>

                    </li>




                    @*<li class="menu-item dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Drop Down<b class="caret"></b></a>
                            <ul class="dropdown-menu">

                                <li class="menu-item"> <a href="#">Link 0</a></li>

                                <li class="menu-item dropdown dropdown-submenu">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Level 1</a>

                                    <ul class="dropdown-menu">

                                        <li class="menu-item ">
                                            <a href="#">Link 1</a>
                                        </li>

                                        <li class="menu-item dropdown dropdown-submenu">
                                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">Level 2</a>
                                            <ul class="dropdown-menu">
                                                <li>
                                                    <a href="#">Link 3</a>
                                                </li>
                                            </ul>
                                        </li>

                                    </ul>
                                </li>

                            </ul>
                        </li>*@



                </ul>
                            //fin barra izquierda

                            //barra derecha
                            <ul class="nav navbar-nav navbar-right">
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-expanded="false"><span class="text-info"><i class="fa fa-male"></i> Hola, </span>@User.Nombre<span class="text-info">! <span class="caret"></span></span></a>
                                    <ul class="dropdown-menu" role="menu">
                                        @Html.MenuItemIcon("<i class='fa fa-street-view'></i> Mi Perfil", "Mi Perfil", "Detalles", "Usuario", new { id = User.UsuarioID })
                                        @Html.MenuItemIcon("<i class='fa fa-user-times'></i> Cerrar Sesión", "Cerrar Sesión", "CerrarSesion", "Usuario")
                                    </ul>
                                </li>
                            </ul>
                //fin barra derecha
            }
            else
            {
                //barra derecha
                <ul class="nav navbar-nav navbar-right">
                    @Html.MenuItemIcon("<i class='fa fa-sign-in'></i> Iniciar Sesión", "Iniciar Sesión", "IniciarSesion", "Usuario", null, new { data_modal = "", @class = "IniciarSesion" })
                </ul>
                //fin barra derecha
            }



        </div><!-- /.navbar-collapse -->
    </div><!-- /.container-fluid -->
</nav>


























<script type="text/javascript">

    $(function () {
        $.ajaxSetup({ cache: false });

        $("a[data-modal].IniciarSesion").on("click", function (e) {

            $('#myModalContent').load(this.href, function () {
                $('#myModal').modal({
                    /*backdrop: 'static',*/
                    keyboard: true
                }, 'show');

                bindForm_Login(this);

            });
            return false;
        });
    });

    function bindForm_Login(dialog) {

        setTimeout(function () {
            $('#UserName').focus();
        }, 500);

        $('form', dialog).submit(function () {
            $.ajax({
                url: this.action,
                type: this.method,
                data: $(this).serialize(),
                success: function (result) {
                    if (result.success) {
                        //$('#myModal').modal('hide');
                        window.location.href = result.url;

                    } else {
                        $('#myModalContent').html(result);
                        bindForm_Login(dialog);
                    }
                }
            });
            return false;
        });
    }
</script>